This folder will contain one file/component per app window, e.g. MyComputer.tsx, Chrome.tsx, Notepad.tsx, Paint.tsx, Doom.tsx, Gemini.tsx, Minesweeper.tsx, MediaPlayer.tsx, ImageViewer.tsx, etc.

Each app component should implement its own UI and logic, and be imported/used in WindowContent.tsx based on the app id.
